** _abc_ means underline **

Relations obtained from E/R diagram

  Person(_medicaidNum_, telephone, address, LastName, firstName, city, province,
    postalCode, citizenship, email);

  Infection(_dateInfection_, _medicaidNum_);

  AgeGroup(_ageRange_, isCurrent);

  Vaccination(_medicaidNum_, _doseNumber_, date)

  PersonAgeGroup(_ageRange_, _medicaidNum_);

  VaccinationDrug(_name_, status, dateLastStatus);

  VaccinationDoneWith(_medicaidNum_, _doseNumber_, _name_);

  VaccinationDoneAt(_medicaidNum_, _doseNumber_, _name_, _address_);

  HealthFacility(_name_, _address_, phoneNumber, webAddress, type);
